package ru.netology.sqr;

public class SQRService {
    public int calculate(int num) // какие параметры указывать в методе? Я не понимаю.
    {
        int sum = //.....; // как будет выглядеть переменная, которая будет считать количество чисел, корень которых подпадает в дипазон?
            for (int i = 10; i < 100; i++) {
            if (i * i >= 200 && i * i <= 300) {
              // ЧТо писать дальше? я запутался ,((((
            }
        }
        return 0;
    }
}
